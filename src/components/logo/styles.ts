import styled from "styled-components";

export const Container = styled.div`
  font-size: 1.5rem;
  display: flex;
  align-items: center;
  color: var(--white);
  font-weight: 600;
  p {
    margin-left: 0.4rem;
  }
  b {
    display: flex;
    text-align: center;

    color: var(--orange);
  }
`;
