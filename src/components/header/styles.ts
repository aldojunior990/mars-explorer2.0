import styled from "styled-components";

export const Container = styled.header`
  padding-top: 3rem;
  padding-bottom: 3rem;
  color: var(--white);
  background-color: var(--black);
  width: 100%;
`;
